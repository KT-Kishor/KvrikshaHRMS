<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:u="sap.ui.unified"
    xmlns:f="sap.ui.layout.form"
>

<Dialog
    id="themeDialog"
    title="Theme Settings"
    contentWidth="400px"
>

    <VBox class="sapUiSmallMargin">
        <Label text="Overall Theme"/>
        <Button
            id="colorBtn"
            text="Choose Theme Color"
            press="onOpenThemePalette"
        />
        <Label text="Company Logo" visible="{= ${LoginModel>/Role} === 'Admin' }"/>

        <u:FileUploader
            width="100%"
            change="onLogoUpload"
            buttonText="Upload Logo"
            fileType="jpg,png"
            maximumFileSize="2"
            visible="{= ${LoginModel>/Role} === 'Admin' }"
        />
        <Label text="Text Color"/>
         	<Input id="colorS"
					type="Text"
					width="200px"
					placeholder="Enter Color ..."
					showValueHelp="true"
					valueHelpRequest=".openSimplifiedModeSample"
					change=".handleInputChange"/>

        <Image
            src="{ThemeModel>/logo}"
            width="120px"
            height="120px"
            class="sapUiSmallMarginTop"
        />
        <Label text = "Button Color"/>
          <Input id="colorB"
					type="Text"
					width="200px"
					placeholder="Enter Color ..."
					showValueHelp="true"
					valueHelpRequest=".openSimplifiedModeSample"
					change=".handleInputChange"/>

    </VBox>

    <beginButton>
        <Button
            text="Save"
            press="onSaveTheme"
        />
    </beginButton>

    <endButton>
        <Button
            text="Close"
            press="onCloseFragment"
        />
    </endButton>

</Dialog>

</core:FragmentDefinition>